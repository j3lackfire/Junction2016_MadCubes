using UnityEngine;
using System.Collections;

//Don't know how to make it multishot
public class WaterSniperProjectile : BasicProjectile {

    //public override void Init(bool _isEnemy, int _damage, Vector3 _startPos, Vector3 _targetPos, BaseElementObject _targetObject, GameElement _bulletElement)
    //{
    //    base.Init(_isEnemy, _damage, _startPos, _targetPos, _targetObject, _bulletElement);
    //    Vector3 bulletDirection = (targetPosition - startPosition).normalized;
    //    targetPosition += bulletDirection * 50f;
    //}

    //protected override void CheckReachTarget()
    //{
    //    base.CheckReachTarget();
    //}
}
